<mvc:View
	controllerName="testaroa.controller.Detail"
	xmlns="sap.m"
	xmlns:mvc="sap.ui.core.mvc">
    <!--Pagina detalles-->
	<Page id="paginaDetalle"
		title="{i18n>detailPage}"
		showNavButton="true"
		navButtonPress=".onNavBack"> <!--Boton para volver para atrás-->
        <!--Objecto de la pagina hellopanel como cabecera-->
		<ObjectHeader id="objectPress"
			title="{invoice>ProductName}"
			number="{
				parts: [{path: 'invoice>ExtendedPrice'}, 'view>/currency'],
				type: 'sap.ui.model.type.Currency',
				formatOptions: {
					showMeasure: false
				}
			}"
			numberUnit="{view>/currency}">
		</ObjectHeader>
	</Page>
</mvc:View>